diff --git a/.jules/bolt.md b/.jules/bolt.md
index e880f27..02300ce 100644
--- a/.jules/bolt.md
+++ b/.jules/bolt.md
@@ -5,3 +5,7 @@
 ## 2024-05-23 - Synchronous Audit Logging Bottleneck
 **Learning:** `ToolOrchestrator._audit_action` was performing synchronous file I/O (open/write/close) for every tool invocation. This introduced ~68ms latency per 1000 calls. Moving this to a background thread with `queue.Queue` reduced it to ~3ms (20x improvement).
 **Action:** For high-frequency logging or audit trails, always use an asynchronous writer or background thread to decouple I/O latency from the main execution path.
+
+## 2025-03-05 - Optimize N+1 Query in Entity Similarity Search
+**Learning:** Finding similar entities based on shared relationships using an iterative N+1 loop with separate database calls to fetch relationships for each entity leads to severe performance degradation as the graph grows. Comparing all entities with `SELECT * FROM ... WHERE name != :name` and pulling relationships into Python memory for set operations is extremely inefficient and slow (O(N) queries where N is the number of entities).
+**Action:** When calculating similarity like Jaccard index between graph entities, use SQL Common Table Expressions (CTEs) to perform the computation entirely within the database. Implement an intersection-first approach by identifying only candidates sharing at least one relationship (`JOIN` on relations), calculating intersection and union counts via `GROUP BY`, and mathematically deriving the similarity score in the final `SELECT`. This avoids N+1 database roundtrips, significantly reducing I/O and latency.
diff --git a/infra/src/mnemosyne/graph/manager.py b/infra/src/mnemosyne/graph/manager.py
index bd2c8f9..3acdca2 100644
--- a/infra/src/mnemosyne/graph/manager.py
+++ b/infra/src/mnemosyne/graph/manager.py
@@ -367,36 +367,59 @@ class GraphManager:
                 if ref_total == 0:
                     return []

-                # Compare with all other entities
-                all_entities_stmt = text("SELECT id, name FROM kg_entities WHERE name != :name")
-                all_entities_res = await session.execute(all_entities_stmt, {"name": entity_name})
-
-                # Get reference entity relations
-                ref_rels_stmt = text("SELECT target_id, relation FROM kg_relations WHERE source_id = :ref_id")
-                ref_rels_res = await session.execute(ref_rels_stmt, {"ref_id": ref_id})
-                ref_rels = set((r[0], r[1]) for r in ref_rels_res.fetchall())
-
-                for entity_id, entity_name_cmp in all_entities_res.fetchall():
-                    # Get this entity's relationships
-                    cmp_stmt = text("""
-                        SELECT target_id, relation FROM kg_relations WHERE source_id = :eid
-                    """)
-                    cmp_res = await session.execute(cmp_stmt, {"eid": entity_id})
-                    cmp_rels = set((r[0], r[1]) for r in cmp_res.fetchall())
-
-                    intersection = len(ref_rels.intersection(cmp_rels))
-                    union = ref_total + len(cmp_rels) - intersection
-
-                    if union > 0:
-                        similarity = intersection / union
-                        if similarity >= similarity_threshold:
-                            similar_entities.append({
-                                "entity": entity_name_cmp,
-                                "similarity": similarity,
-                                "shared_relations": intersection
-                            })
-
-                similar_entities.sort(key=lambda x: x["similarity"], reverse=True)
+                # Intersection-first optimization using CTEs
+                # 1. Identify candidates (entities sharing >=1 relation) -> O(Neighbors)
+                # 2. Count totals for candidates only
+                # 3. Calculate Jaccard in SQL
+                # Note: This query avoids full table scans of unrelated entities.
+                stmt = text("""
+                    WITH ref_rels AS (
+                        SELECT target_id, relation
+                        FROM kg_relations
+                        WHERE source_id = :ref_id
+                    ),
+                    candidates AS (
+                        SELECT
+                            r.source_id,
+                            COUNT(r.id) as intersection_cnt
+                        FROM kg_relations r
+                        JOIN ref_rels rr ON r.target_id = rr.target_id AND r.relation = rr.relation
+                        WHERE r.source_id != :ref_id
+                        GROUP BY r.source_id
+                        HAVING COUNT(r.id) >= :ref_total * :threshold
+                    ),
+                    candidate_totals AS (
+                        SELECT
+                            r.source_id,
+                            COUNT(r.id) as total_cnt
+                        FROM kg_relations r
+                        JOIN candidates c ON r.source_id = c.source_id
+                        GROUP BY r.source_id
+                    )
+                    SELECT
+                        e.name,
+                        c.intersection_cnt,
+                        t.total_cnt,
+                        (CAST(c.intersection_cnt AS FLOAT) / (t.total_cnt + :ref_total - c.intersection_cnt)) as similarity
+                    FROM candidates c
+                    JOIN candidate_totals t ON c.source_id = t.source_id
+                    JOIN kg_entities e ON c.source_id = e.id
+                    WHERE (CAST(c.intersection_cnt AS FLOAT) / (t.total_cnt + :ref_total - c.intersection_cnt)) >= :threshold
+                    ORDER BY similarity DESC
+                """)
+
+                res = await session.execute(stmt, {
+                    "ref_id": ref_id,
+                    "ref_total": ref_total,
+                    "threshold": similarity_threshold
+                })
+
+                for row in res.fetchall():
+                    similar_entities.append({
+                        "entity": row[0],
+                        "similarity": row[3],
+                        "shared_relations": row[1]
+                    })

             return similar_entities

diff --git a/patch_unoptimized.diff b/patch_unoptimized.diff
deleted file mode 100644
index dfd7d58..0000000
--- a/patch_unoptimized.diff
+++ /dev/null
@@ -1,86 +0,0 @@
-<<<<<<< SEARCH
-                # Intersection-first optimization using CTEs
-                # 1. Identify candidates (entities sharing >=1 relation) -> O(Neighbors)
-                # 2. Count totals for candidates only
-                # 3. Calculate Jaccard in SQL
-                # Note: This query avoids full table scans of unrelated entities.
-                stmt = text("""
-                    WITH ref_rels AS (
-                        SELECT target_id, relation
-                        FROM kg_relations
-                        WHERE source_id = :ref_id
-                    ),
-                    candidates AS (
-                        SELECT
-                            r.source_id,
-                            COUNT(r.id) as intersection_cnt
-                        FROM kg_relations r
-                        JOIN ref_rels rr ON r.target_id = rr.target_id AND r.relation = rr.relation
-                        WHERE r.source_id != :ref_id
-                        GROUP BY r.source_id
-                        HAVING COUNT(r.id) >= :ref_total * :threshold
-                    ),
-                    candidate_totals AS (
-                        SELECT
-                            r.source_id,
-                            COUNT(r.id) as total_cnt
-                        FROM kg_relations r
-                        JOIN candidates c ON r.source_id = c.source_id
-                        GROUP BY r.source_id
-                    )
-                    SELECT
-                        e.name,
-                        c.intersection_cnt,
-                        t.total_cnt,
-                        (CAST(c.intersection_cnt AS FLOAT) / (t.total_cnt + :ref_total - c.intersection_cnt)) as similarity
-                    FROM candidates c
-                    JOIN candidate_totals t ON c.source_id = t.source_id
-                    JOIN kg_entities e ON c.source_id = e.id
-                    WHERE (CAST(c.intersection_cnt AS FLOAT) / (t.total_cnt + :ref_total - c.intersection_cnt)) >= :threshold
-                    ORDER BY similarity DESC
-                """)
-
-                res = await session.execute(stmt, {
-                    "ref_id": ref_id,
-                    "ref_total": ref_total,
-                    "threshold": similarity_threshold
-                })
-
-                for row in res.fetchall():
-                    similar_entities.append({
-                        "entity": row[0],
-                        "similarity": row[3],
-                        "shared_relations": row[1]
-                    })
-=======
-                # Compare with all other entities
-                all_entities_stmt = text("SELECT id, name FROM kg_entities WHERE name != :name")
-                all_entities_res = await session.execute(all_entities_stmt, {"name": entity_name})
-
-                # Get reference entity relations
-                ref_rels_stmt = text("SELECT target_id, relation FROM kg_relations WHERE source_id = :ref_id")
-                ref_rels_res = await session.execute(ref_rels_stmt, {"ref_id": ref_id})
-                ref_rels = set((r[0], r[1]) for r in ref_rels_res.fetchall())
-
-                for entity_id, entity_name_cmp in all_entities_res.fetchall():
-                    # Get this entity's relationships
-                    cmp_stmt = text("""
-                        SELECT target_id, relation FROM kg_relations WHERE source_id = :eid
-                    """)
-                    cmp_res = await session.execute(cmp_stmt, {"eid": entity_id})
-                    cmp_rels = set((r[0], r[1]) for r in cmp_res.fetchall())
-
-                    intersection = len(ref_rels.intersection(cmp_rels))
-                    union = ref_total + len(cmp_rels) - intersection
-
-                    if union > 0:
-                        similarity = intersection / union
-                        if similarity >= similarity_threshold:
-                            similar_entities.append({
-                                "entity": entity_name_cmp,
-                                "similarity": similarity,
-                                "shared_relations": intersection
-                            })
-
-                similar_entities.sort(key=lambda x: x["similarity"], reverse=True)
->>>>>>> REPLACE
diff --git a/src/ippoc/mnemosyne/graph/manager.py b/src/ippoc/mnemosyne/graph/manager.py
index 2d01a4e..b80121c 100644
--- a/src/ippoc/mnemosyne/graph/manager.py
+++ b/src/ippoc/mnemosyne/graph/manager.py
@@ -367,36 +367,59 @@ class GraphManager:
                 if ref_total == 0:
                     return []

-                # Compare with all other entities
-                all_entities_stmt = text("SELECT id, name FROM kg_entities WHERE name != :name")
-                all_entities_res = await session.execute(all_entities_stmt, {"name": entity_name})
-
-                # Get reference entity relations
-                ref_rels_stmt = text("SELECT target_id, relation FROM kg_relations WHERE source_id = :ref_id")
-                ref_rels_res = await session.execute(ref_rels_stmt, {"ref_id": ref_id})
-                ref_rels = set((r[0], r[1]) for r in ref_rels_res.fetchall())
-
-                for entity_id, entity_name_cmp in all_entities_res.fetchall():
-                    # Get this entity's relationships
-                    cmp_stmt = text("""
-                        SELECT target_id, relation FROM kg_relations WHERE source_id = :eid
-                    """)
-                    cmp_res = await session.execute(cmp_stmt, {"eid": entity_id})
-                    cmp_rels = set((r[0], r[1]) for r in cmp_res.fetchall())
-
-                    intersection = len(ref_rels.intersection(cmp_rels))
-                    union = ref_total + len(cmp_rels) - intersection
-
-                    if union > 0:
-                        similarity = intersection / union
-                        if similarity >= similarity_threshold:
-                            similar_entities.append({
-                                "entity": entity_name_cmp,
-                                "similarity": similarity,
-                                "shared_relations": intersection
-                            })
-
-                similar_entities.sort(key=lambda x: x["similarity"], reverse=True)
+                # Intersection-first optimization using CTEs
+                # 1. Identify candidates (entities sharing >=1 relation) -> O(Neighbors)
+                # 2. Count totals for candidates only
+                # 3. Calculate Jaccard in SQL
+                # Note: This query avoids full table scans of unrelated entities.
+                stmt = text("""
+                    WITH ref_rels AS (
+                        SELECT target_id, relation
+                        FROM kg_relations
+                        WHERE source_id = :ref_id
+                    ),
+                    candidates AS (
+                        SELECT
+                            r.source_id,
+                            COUNT(r.id) as intersection_cnt
+                        FROM kg_relations r
+                        JOIN ref_rels rr ON r.target_id = rr.target_id AND r.relation = rr.relation
+                        WHERE r.source_id != :ref_id
+                        GROUP BY r.source_id
+                        HAVING COUNT(r.id) >= :ref_total * :threshold
+                    ),
+                    candidate_totals AS (
+                        SELECT
+                            r.source_id,
+                            COUNT(r.id) as total_cnt
+                        FROM kg_relations r
+                        JOIN candidates c ON r.source_id = c.source_id
+                        GROUP BY r.source_id
+                    )
+                    SELECT
+                        e.name,
+                        c.intersection_cnt,
+                        t.total_cnt,
+                        (CAST(c.intersection_cnt AS FLOAT) / (t.total_cnt + :ref_total - c.intersection_cnt)) as similarity
+                    FROM candidates c
+                    JOIN candidate_totals t ON c.source_id = t.source_id
+                    JOIN kg_entities e ON c.source_id = e.id
+                    WHERE (CAST(c.intersection_cnt AS FLOAT) / (t.total_cnt + :ref_total - c.intersection_cnt)) >= :threshold
+                    ORDER BY similarity DESC
+                """)
+
+                res = await session.execute(stmt, {
+                    "ref_id": ref_id,
+                    "ref_total": ref_total,
+                    "threshold": similarity_threshold
+                })
+
+                for row in res.fetchall():
+                    similar_entities.append({
+                        "entity": row[0],
+                        "similarity": row[3],
+                        "shared_relations": row[1]
+                    })

             return similar_entities

diff --git a/test_perf.py b/test_perf.py
deleted file mode 100644
index 7a3807b..0000000
--- a/test_perf.py
+++ /dev/null
@@ -1,19 +0,0 @@
-import asyncio
-from src.ippoc.mnemosyne.graph.manager import GraphManager
-
-async def main():
-    gm = GraphManager("sqlite+aiosqlite:///:memory:")
-    await gm.init_db()
-
-    # Create dummy graph
-    await gm.add_triple("EntityA", "relates_to", "EntityB")
-    await gm.add_triple("EntityA", "relates_to", "EntityC")
-    await gm.add_triple("EntityD", "relates_to", "EntityB")
-    await gm.add_triple("EntityD", "relates_to", "EntityC")
-
-    # Find similar
-    sim = await gm.find_similar_entities("EntityA", similarity_threshold=0.1)
-    print(f"Similar to A: {sim}")
-
-if __name__ == "__main__":
-    asyncio.run(main())
diff --git a/test_perf2.py b/test_perf2.py
deleted file mode 100644
index 1cd9c10..0000000
--- a/test_perf2.py
+++ /dev/null
@@ -1,36 +0,0 @@
-import asyncio
-import time
-from src.ippoc.mnemosyne.graph.manager import GraphManager
-
-async def main():
-    gm = GraphManager("sqlite+aiosqlite:///:memory:")
-    await gm.init_db()
-
-    print("Building large graph...")
-    # Target entity
-    await gm.add_triple("TargetEntity", "has_property", "Prop1")
-    await gm.add_triple("TargetEntity", "has_property", "Prop2")
-    await gm.add_triple("TargetEntity", "has_property", "Prop3")
-    await gm.add_triple("TargetEntity", "has_property", "Prop4")
-    await gm.add_triple("TargetEntity", "has_property", "Prop5")
-
-    # Other entities (will create 1000 entities, each with 5 random properties, some overlapping)
-    import random
-    random.seed(42)
-    props = [f"Prop{i}" for i in range(1, 50)]
-
-    for i in range(500):
-        ent_name = f"Entity_{i}"
-        ent_props = random.sample(props, 5)
-        for p in ent_props:
-            await gm.add_triple(ent_name, "has_property", p)
-
-    print("Testing original method vs new method...")
-    start = time.time()
-    sim = await gm.find_similar_entities("TargetEntity", similarity_threshold=0.1)
-    end = time.time()
-    print(f"Time taken: {end - start:.4f}s")
-    print(f"Found {len(sim)} similar entities")
-
-if __name__ == "__main__":
-    asyncio.run(main())
diff --git a/test_perf_manager.py b/test_perf_manager.py
deleted file mode 100644
index db5d04a..0000000
--- a/test_perf_manager.py
+++ /dev/null
@@ -1,87 +0,0 @@
-import asyncio
-import time
-from sqlalchemy import text
-from src.ippoc.mnemosyne.graph.manager import GraphManager
-
-async def test_slow(gm, entity_name, similarity_threshold):
-    # This simulates the slow N+1 code path
-    similar_entities = []
-    async with gm.Session() as session:
-        ref_id_stmt = text("SELECT id FROM kg_entities WHERE name = :name")
-        ref_id_res = await session.execute(ref_id_stmt, {"name": entity_name})
-        ref_row = ref_id_res.fetchone()
-        if not ref_row: return []
-        ref_id = ref_row[0]
-
-        ref_rels_stmt = text("SELECT target_id, relation FROM kg_relations WHERE source_id = :ref_id")
-        ref_rels_res = await session.execute(ref_rels_stmt, {"ref_id": ref_id})
-        ref_rels = set((r[0], r[1]) for r in ref_rels_res.fetchall())
-        ref_total = len(ref_rels)
-        if ref_total == 0: return []
-
-        all_entities_stmt = text("SELECT id, name FROM kg_entities WHERE name != :name")
-        all_entities_res = await session.execute(all_entities_stmt, {"name": entity_name})
-
-        for entity_id, entity_name_cmp in all_entities_res.fetchall():
-            cmp_stmt = text("SELECT target_id, relation FROM kg_relations WHERE source_id = :eid")
-            cmp_res = await session.execute(cmp_stmt, {"eid": entity_id})
-            cmp_rels = set((r[0], r[1]) for r in cmp_res.fetchall())
-
-            intersection = len(ref_rels.intersection(cmp_rels))
-            union = ref_total + len(cmp_rels) - intersection
-
-            if union > 0:
-                similarity = intersection / union
-                if similarity >= similarity_threshold:
-                    similar_entities.append({
-                        "entity": entity_name_cmp,
-                        "similarity": similarity,
-                        "shared_relations": intersection
-                    })
-
-        similar_entities.sort(key=lambda x: x["similarity"], reverse=True)
-        return similar_entities
-
-async def test_fast(gm, entity_name, similarity_threshold):
-    # This calls the current optimized implementation
-    return await gm.find_similar_entities(entity_name, similarity_threshold)
-
-async def main():
-    gm = GraphManager("sqlite+aiosqlite:///:memory:")
-    await gm.init_db()
-
-    print("Building large graph...")
-    await gm.add_triple("TargetEntity", "has_property", "Prop1")
-    await gm.add_triple("TargetEntity", "has_property", "Prop2")
-    await gm.add_triple("TargetEntity", "has_property", "Prop3")
-    await gm.add_triple("TargetEntity", "has_property", "Prop4")
-    await gm.add_triple("TargetEntity", "has_property", "Prop5")
-
-    import random
-    random.seed(42)
-    props = [f"Prop{i}" for i in range(1, 50)]
-
-    for i in range(500):
-        ent_name = f"Entity_{i}"
-        ent_props = random.sample(props, 5)
-        for p in ent_props:
-            await gm.add_triple(ent_name, "has_property", p)
-
-    print("Measuring SLOW (N+1) version...")
-    start = time.time()
-    sim_slow = await test_slow(gm, "TargetEntity", 0.1)
-    end = time.time()
-    time_slow = end - start
-    print(f"Time taken (slow): {time_slow:.4f}s")
-
-    print("Measuring FAST (Optimized) version...")
-    start = time.time()
-    sim_fast = await test_fast(gm, "TargetEntity", 0.1)
-    end = time.time()
-    time_fast = end - start
-    print(f"Time taken (fast): {time_fast:.4f}s")
-
-    print(f"Improvement: {time_slow / time_fast:.2f}x faster")
-
-if __name__ == "__main__":
-    asyncio.run(main())
